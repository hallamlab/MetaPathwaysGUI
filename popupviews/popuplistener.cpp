#include "popuplistener.h"

PopupListener::PopupListener(QWidget *parent) :
    QWidget(parent)
{
}


void PopupListener::clickedOnCell(int row, int col) {

//    qDebug() << "Row " << row << " col " << col;
}
